<UserControl
    x:Class="Voltflow.Views.Pages.Map.SidePanels.StationInformationView"
    xmlns="https://github.com/avaloniaui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:iconPacks="https://github.com/MahApps/IconPacks.Avalonia"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:sidePanels="clr-namespace:Voltflow.ViewModels.Pages.Map.SidePanels"
    mc:Ignorable="d">
    <Design.DataContext>
        <sidePanels:StationInformationViewModel />
    </Design.DataContext>

    <StackPanel>
        <TextBlock
            HorizontalAlignment="Center"
            Classes="size-sm color-gray weight-bold"
            Text="{Binding ViewTitle}" />

        <Separator />

        <TextBlock Classes="size-sm color-gray weight-bold" Text="Status" />
        <TextBlock Classes="size-sm color-gray" Text="{Binding Status}" />

        <TextBlock Classes="size-sm color-gray weight-bold" Text="Cost" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Classes="size-sm color-gray"
                Text="{Binding Cost}" />
            <TextBlock
                Grid.Column="1"
                Classes="size-sm color-gray"
                Text="zł" />
        </Grid>


        <TextBlock Classes="size-sm color-gray weight-bold" Text="Maximum Charging Rate" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Column="0"
                Classes="size-sm color-gray"
                Text="{Binding MaxChargeRate}" />
            <TextBlock
                Grid.Column="1"
                Classes="size-sm color-gray"
                Text="kWh" />
        </Grid>

        <Separator />

        <Button
            HorizontalAlignment="Stretch"
            Classes="Success"
            Command="{Binding Charge}"
            IsEnabled="{Binding Selected}">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconBootstrapIcons VerticalAlignment="Center" Kind="LightningChargeFill" />
                <TextBlock VerticalAlignment="Center" Text="Charge" />
            </StackPanel>
        </Button>
    </StackPanel>
</UserControl>
